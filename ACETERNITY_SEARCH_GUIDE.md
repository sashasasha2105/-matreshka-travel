# 🔍 Aceternity-Style Search Component

## Обзор

Красивый компонент поиска в стиле **Aceternity UI** для проекта Матрешка. Реализован на **чистом JavaScript** без React, с плавными анимациями и эффектом vanish.

## ✨ Особенности

- ✅ **Без React** - работает на vanilla JavaScript
- 🎨 **Glass morphism** дизайн в стиле Aceternity UI
- ⌨️ **Анимированные placeholder'ы** - автоматическая ротация примеров
- 🎬 **Vanish эффект** - красивое исчезновение при поиске
- 🌓 **Dark mode** поддержка
- 📱 **Полностью адаптивный** дизайн
- ⚡ **Плавные анимации** на чистом CSS
- 🔄 **Интеграция** с существующим поиском

## 📦 Файлы

```
styles/aceternity-search.css    # Стили компонента
scripts/aceternity-search.js    # JavaScript логика
```

## 🚀 Быстрый старт

### 1. Подключение

В `index.html` уже подключены все необходимые файлы:

```html
<!-- CSS -->
<link rel="stylesheet" href="styles/aceternity-search.css?v=999">

<!-- JavaScript -->
<script src="scripts/aceternity-search.js" defer></script>
```

### 2. HTML контейнер

Создайте контейнер, куда будет рендериться компонент:

```html
<div id="aceternitySearchContainer"></div>
```

### 3. Инициализация

В `scripts/script.js` добавьте инициализацию:

```javascript
// В DOMContentLoaded или после загрузки страницы
new AceternitySearch('aceternitySearchContainer', {
    placeholders: [
        "Куда поедем в России? 🌍",
        "Например: Байкал ⛰️",
        "Например: Сочи 🏖️",
        "Например: Казань 🕌"
    ],
    placeholderInterval: 3000,
    onSearch: (query) => {
        console.log('Поиск:', query);
        // Ваша логика поиска
        searchRegions(query);
    },
    vanishOnSubmit: true,
    buttonText: "Найти",
    minLength: 2
});
```

## ⚙️ Параметры конфигурации

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `placeholders` | `Array<string>` | `["Куда поедем в России? 🌍", ...]` | Массив текстов для ротации в placeholder |
| `placeholderInterval` | `number` | `3000` | Интервал смены placeholder (мс) |
| `onSearch` | `Function` | `console.log` | Callback при отправке формы, принимает `query` |
| `vanishOnSubmit` | `boolean` | `true` | Включить vanish-эффект при поиске |
| `buttonText` | `string` | `"Найти"` | Текст кнопки поиска |
| `minLength` | `number` | `2` | Минимальная длина запроса |

## 🎨 Кастомизация стилей

### Цвета

Отредактируйте `styles/aceternity-search.css`:

```css
/* Изменить цвет фокуса */
.aceternity-search-input:focus {
    border-color: rgba(59, 130, 246, 0.5); /* Синий по умолчанию */
}

/* Изменить цвет кнопки */
.aceternity-search-button {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
}
```

### Размеры

```css
/* Изменить максимальную ширину */
.aceternity-search-wrapper {
    max-width: 42rem; /* 672px */
}

/* Изменить размер шрифта */
.aceternity-search-input {
    font-size: 1.125rem; /* 18px */
}
```

## 📝 Примеры использования

### Базовый пример

```javascript
const search = new AceternitySearch('mySearchContainer', {
    onSearch: (query) => {
        console.log('Ищем:', query);
    }
});
```

### Без vanish эффекта

```javascript
new AceternitySearch('mySearchContainer', {
    vanishOnSubmit: false,
    onSearch: (query) => {
        // Поиск без исчезновения формы
        performSearch(query);
    }
});
```

### Кастомные placeholder'ы

```javascript
new AceternitySearch('mySearchContainer', {
    placeholders: [
        "Поиск по городам 🏙️",
        "Поиск по достопримечательностям 🗿",
        "Поиск по турам 🎒"
    ],
    placeholderInterval: 2000, // Быстрая смена
    onSearch: searchHandler
});
```

### Интеграция с API

```javascript
new AceternitySearch('searchContainer', {
    onSearch: async (query) => {
        try {
            const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
            const results = await response.json();
            displayResults(results);
        } catch (error) {
            console.error('Ошибка поиска:', error);
        }
    },
    minLength: 3
});
```

## 🎭 Эффекты и анимации

### Vanish Animation

При отправке формы (если `vanishOnSubmit: true`):
1. Форма плавно исчезает с blur эффектом (500ms)
2. Выполняется callback `onSearch`
3. Форма появляется обратно (500ms)
4. Input очищается

### Placeholder Rotation

- Автоматическая смена каждые 3 секунды (настраивается)
- Плавный fade-эффект при смене
- Останавливается при вводе текста
- Возобновляется при очистке input

### Focus Effects

- Scale анимация (1.01x)
- Светящееся кольцо вокруг input
- Smooth transitions на всех элементах

## 🔧 API методы

```javascript
const search = new AceternitySearch('container', options);

// Уничтожить компонент
search.destroy();

// Остановить ротацию placeholder
search.stopPlaceholderRotation();

// Запустить ротацию placeholder
search.startPlaceholderRotation();
```

## 🐛 Решение проблем

### Компонент не появляется

```javascript
// Проверьте, что контейнер существует
const container = document.getElementById('aceternitySearchContainer');
console.log(container); // Должен быть элемент

// Проверьте, что скрипт загружен
console.log(typeof AceternitySearch); // Должно быть 'function'
```

### Стили не применяются

```html
<!-- Убедитесь, что CSS подключен -->
<link rel="stylesheet" href="styles/aceternity-search.css?v=999">

<!-- Проверьте путь к файлу -->
<!-- В консоли браузера не должно быть ошибок 404 -->
```

### onSearch не вызывается

```javascript
// Проверьте минимальную длину
new AceternitySearch('container', {
    minLength: 2, // Убедитесь, что вводите >= 2 символов
    onSearch: (query) => {
        console.log('Вызвано!', query);
    }
});
```

## 📱 Адаптивность

Компонент автоматически адаптируется под разные экраны:

- **Desktop** (>768px): полный размер, крупный шрифт
- **Tablet** (481-768px): средний размер
- **Mobile** (<480px): компактный вид, оптимизированные отступы

## 🌙 Dark Mode

Компонент автоматически определяет тему системы через `prefers-color-scheme`:

```css
@media (prefers-color-scheme: dark) {
    /* Dark theme стили автоматически применяются */
}
```

## 🎯 Интеграция в Матрешку

В проекте Матрешка компонент интегрирован так:

```javascript
// scripts/script.js:1532
new AceternitySearch('aceternitySearchContainer', {
    placeholders: [/* список российских городов */],
    onSearch: (query) => {
        // Используем существующую функцию поиска
        if (typeof searchRegions === 'function') {
            searchRegions(query);
        }
    },
    vanishOnSubmit: true
});
```

## 📚 Дополнительно

- Компонент использует **чистый CSS** для всех анимаций (без external библиотек)
- **Легковесный** - минимальные зависимости
- **Производительный** - использует CSS transforms для анимаций
- **Доступный** - работает со всеми браузерами с поддержкой CSS3

---

**Made with ❤️ for Матрешка Travel App**

*Вдохновлено дизайном [Aceternity UI](https://ui.aceternity.com/)*
