# 🚀 Автозапуск сервера Матрешка

## ✅ Автозапуск настроен!

Сервер Матрешка теперь **автоматически запускается** при включении твоего MacBook.

---

## 📋 Что было настроено:

### 1. **LaunchAgent** (`com.matreshka.server`)
- Автоматический запуск при загрузке macOS
- Автоматический перезапуск при падении сервера
- Логирование всех событий

### 2. **Скрипт запуска** (`start_server.sh`)
- Убивает старые процессы на порту 8000
- Запускает HTTP сервер Python
- Пишет логи в `server.log`

### 3. **Скрипт управления** (`manage_server.sh`)
- Удобное управление сервером из терминала

---

## 🎛️ Управление сервером

### Основные команды:

```bash
# Перейти в директорию проекта
cd /Users/alexbrizkiy/PycharmProjects/PythonProject20

# Проверить статус
./manage_server.sh status

# Запустить сервер (если остановлен)
./manage_server.sh start

# Остановить сервер
./manage_server.sh stop

# Перезапустить сервер
./manage_server.sh restart

# Посмотреть логи
./manage_server.sh logs
```

### Управление автозапуском:

```bash
# Отключить автозапуск (сервер не будет запускаться при включении Mac)
./manage_server.sh disable-autostart

# Включить автозапуск обратно
./manage_server.sh enable-autostart
```

---

## 🌐 Доступ к сайту

После включения MacBook сервер автоматически запустится через ~5-10 секунд.

**Сайт доступен по адресу:**
- 🌐 **http://localhost:8000**
- 🌐 **http://127.0.0.1:8000**

---

## 📊 Проверка работы

### Способ 1: Через браузер
Открой в браузере: http://localhost:8000

### Способ 2: Через терминал
```bash
cd /Users/alexbrizkiy/PycharmProjects/PythonProject20
./manage_server.sh status
```

Ты должен увидеть:
```
📊 Статус сервера:
  🟢 LaunchAgent: АКТИВЕН
  🟢 HTTP сервер: РАБОТАЕТ (http://localhost:8000)
  📍 PID: XXXXX
```

---

## 📝 Логи

### Файлы логов:
- `server.log` - основные логи
- `server_stdout.log` - stdout Python сервера
- `server_stderr.log` - ошибки (если есть)

### Посмотреть логи:
```bash
# Последние 20 строк
./manage_server.sh logs

# Следить за логами в реальном времени
tail -f server.log
```

---

## 🔧 Расположение файлов

```
/Users/alexbrizkiy/PycharmProjects/PythonProject20/
├── start_server.sh              # Скрипт запуска сервера
├── manage_server.sh             # Скрипт управления
├── server.log                   # Логи сервера
├── server_stdout.log            # Stdout
├── server_stderr.log            # Stderr
└── server.pid                   # PID процесса (если используется)

/Users/alexbrizkiy/Library/LaunchAgents/
└── com.matreshka.server.plist   # LaunchAgent конфигурация
```

---

## ❓ Решение проблем

### Сервер не запускается автоматически:
```bash
# Проверить статус LaunchAgent
launchctl list | grep matreshka

# Если не найден - загрузить вручную:
launchctl load /Users/alexbrizkiy/Library/LaunchAgents/com.matreshka.server.plist
```

### Порт 8000 занят:
```bash
# Узнать, какой процесс использует порт
lsof -ti:8000

# Убить процесс
kill -9 $(lsof -ti:8000)

# Перезапустить сервер
./manage_server.sh restart
```

### Сервер упал:
LaunchAgent автоматически перезапустит сервер через 10 секунд.

Проверить логи:
```bash
./manage_server.sh logs
cat server_stderr.log
```

---

## 🗑️ Удаление автозапуска

Если захочешь удалить автозапуск полностью:

```bash
# Остановить и выгрузить LaunchAgent
launchctl unload /Users/alexbrizkiy/Library/LaunchAgents/com.matreshka.server.plist

# Удалить файл LaunchAgent
rm /Users/alexbrizkiy/Library/LaunchAgents/com.matreshka.server.plist

# Готово! Автозапуск удален
```

---

## 🎉 Готово!

Теперь при каждом включении MacBook сервер Матрешка будет автоматически запускаться на http://localhost:8000

**Ничего делать не нужно - просто включи Mac и открой сайт!** 🚀
