# 🚨 ИНСТРУКЦИЯ ПО ДЕМО-СТРАНИЦАМ

## ✅ РАБОЧИЕ ФАЙЛЫ:

### 1. **aceternity_WORKING.html** ← ЭТОТ РАБОТАЕТ 100%
```bash
open aceternity_WORKING.html
```

Что внутри:
- ✅ Spotlight Cards (3 карточки)
- ✅ Shimmer Buttons (3 кнопки)
- ✅ Text Generate Effect
- ✅ С обработкой ошибок
- ✅ БЕЗ defer (синхронная загрузка)

---

### 2. **test_ultra_simple.html** ← ТЕСТ ЗАГРУЗКИ
```bash
open test_ultra_simple.html
```

Проверяет:
- Загружается ли `animations.js`
- Загружается ли `aceternity-adapter.js`
- Работает ли `ShimmerButton`

---

### 3. **test_simple.html** ← ДИАГНОСТИКА
```bash
open test_simple.html
```

Показывает:
- Какие утилиты загружены
- Какие компоненты загружены
- Ошибки при создании

---

## ❌ ЕСЛИ НЕ РАБОТАЕТ:

### Проблема: "Карточки не отображаются"

**Решение:**

1. Открой **консоль браузера** (F12 или Cmd+Option+I)

2. Проверь ошибки:
   - Красные ошибки в консоли?
   - "ReferenceError: AceternityComponent is not defined"?
   - "ReferenceError: cn is not defined"?

3. Проверь порядок загрузки:
   ```
   ✅ Правильно:
   1. animations.js
   2. aceternity-adapter.js
   3. компонент.js

   ❌ Неправильно:
   1. компонент.js  <- ОШИБКА! Слишком рано
   2. animations.js
   3. aceternity-adapter.js
   ```

---

## 🔧 КАК ПОЧИНИТЬ ДРУГИЕ ДЕМО:

### Если `aceternity_showcase.html` не работает:

1. Убери `defer` из скриптов:
   ```html
   <!-- БЫЛО -->
   <script src="scripts/utils/animations.js" defer></script>

   <!-- СТАЛО -->
   <script src="scripts/utils/animations.js"></script>
   ```

2. Проверь порядок:
   ```html
   <!-- 1. Сначала утилиты -->
   <script src="scripts/utils/animations.js"></script>
   <script src="scripts/utils/aceternity-adapter.js"></script>

   <!-- 2. Потом компоненты -->
   <script src="components/aceternity/spotlight-card.js"></script>
   <script src="components/aceternity/shimmer-button.js"></script>

   <!-- 3. Последним инициализация -->
   <script>
   // Твой код создания компонентов
   </script>
   ```

---

## 📝 CHECKLIST ОТЛАДКИ:

- [ ] Открыл консоль браузера (F12)
- [ ] Проверил красные ошибки
- [ ] Проверил что `typeof AceternityComponent !== 'undefined'`
- [ ] Проверил что `typeof cn !== 'undefined'`
- [ ] Проверил что `typeof motion !== 'undefined'`
- [ ] Убедился что скрипты без `defer`
- [ ] Убедился в правильном порядке загрузки
- [ ] Попробовал `aceternity_WORKING.html`

---

## 🎯 БЫСТРЫЙ ТЕСТ:

Открой консоль и введи:

```javascript
// Проверка утилит
console.log('motion:', typeof motion);
console.log('AceternityComponent:', typeof AceternityComponent);
console.log('cn:', typeof cn);

// Проверка компонентов
console.log('SpotlightCard:', typeof SpotlightCard);
console.log('ShimmerButton:', typeof ShimmerButton);

// Попытка создать
new ShimmerButton('test', {
    text: 'TEST',
    onClick: () => alert('WORKS!')
});
```

Если все `'function'` - значит загружено правильно!

---

## 💡 СОВЕТ:

**Всегда используй `aceternity_WORKING.html` как основу**

Скопируй его и модифицируй под свои нужды:

```bash
cp aceternity_WORKING.html my_custom_demo.html
```

Потом редактируй `my_custom_demo.html`

---

## 🐛 ТИПИЧНЫЕ ОШИБКИ:

### 1. "AceternityComponent is not defined"
**Причина:** `aceternity-adapter.js` не загружен или загружен после компонента
**Решение:** Проверь порядок и убери `defer`

### 2. "cn is not defined"
**Причина:** То же самое - `aceternity-adapter.js` не загружен
**Решение:** То же самое

### 3. "Cannot read property 'appendChild' of null"
**Причина:** Контейнер с указанным ID не существует
**Решение:** Проверь что `<div id="твой-id"></div>` есть в HTML

### 4. "Компонент создается но ничего не показывается"
**Причина:** Tailwind CSS не загружен или стили не применяются
**Решение:** Добавь `<script src="https://cdn.tailwindcss.com"></script>`

---

## 📞 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО:

1. Открой `aceternity_WORKING.html`
2. Открой консоль (F12)
3. Сделай скриншот консоли
4. Опиши что именно не работает

---

Made with 😤 after fixing bugs
