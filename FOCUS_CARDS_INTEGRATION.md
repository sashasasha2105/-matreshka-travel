# 🎨 FocusCards Integration

## Что сделано?

Карточки регионов в проекте Матрешка теперь используют компонент **FocusCards** из Aceternity UI!

---

## ✨ Что такое FocusCards?

**FocusCards** - красивый компонент из Aceternity UI с эффектом фокуса:
- При наведении на одну карточку, остальные **размываются** и **уменьшаются**
- Создает **эффект глубины** и привлекает внимание к выбранной карточке
- **Плавные анимации** и переходы

Источник: https://ui.aceternity.com/components/focus-cards

---

## 📦 Созданные файлы

### 1. Компонент FocusCards
📁 `components/aceternity/focus-cards.js`
- Адаптирован из React в Vanilla JS
- Наследуется от `AceternityComponent`
- Поддерживает динамическое добавление карточек
- Адаптивная сетка (1/2/3 колонки)

### 2. Обновлен scripts/script.js
- Функция `loadRegions()` теперь использует FocusCards
- Сохранена совместимость со старой системой (fallback)
- Добавлена переменная `focusCardsInstance`

### 3. Обновлен index.html
- Подключены утилиты: `animations.js` и `aceternity-adapter.js`
- Подключен компонент: `focus-cards.js`

---

## 🎯 Как это работает?

### 1. Загрузка данных
Регионы загружаются из `window.RUSSIA_REGIONS_DATA`

### 2. Преобразование данных
```javascript
const cardsData = regionsToShow.map(region => ({
    title: region.name,
    src: region.image,
    id: region.id,
    region: region
}));
```

### 3. Создание FocusCards
```javascript
focusCardsInstance = new FocusCards('regionsGrid', {
    cards: cardsData,
    columns: { sm: 1, md: 2, lg: 3 },
    onCardClick: (card) => {
        showRegionDetails(card.id);
    },
    showTitleOnHover: true,
    imageHeight: { sm: '15rem', md: '24rem' }
});
```

### 4. Динамическое добавление
При нажатии "Показать больше":
```javascript
focusCardsInstance.addCards(newCardsData);
```

---

## 🎨 Опции компонента

| Опция | Тип | Описание |
|-------|-----|----------|
| `cards` | `Array` | Массив карточек с `title`, `src`, `id` |
| `columns` | `Object` | Количество колонок: `{ sm: 1, md: 2, lg: 3 }` |
| `onCardClick` | `Function` | Callback при клике на карточку |
| `showTitleOnHover` | `Boolean` | Показывать заголовок при наведении |
| `blurAmount` | `String` | Размытие неактивных карточек (default: `8px`) |
| `scaleInactive` | `Number` | Масштаб неактивных карточек (default: `0.98`) |
| `imageHeight` | `Object` | Высота изображений: `{ sm: '15rem', md: '24rem' }` |

---

## 💡 Эффекты

### При наведении на карточку:
1. **Текущая карточка**:
   - Остается в нормальном размере (`scale: 1`)
   - Без размытия
   - Показывается заголовок (overlay opacity: 1)

2. **Остальные карточки**:
   - Уменьшаются до `0.98`
   - Размываются на `8px`
   - Заголовок скрыт (overlay opacity: 0)

3. **Переходы**:
   - Плавная анимация `300ms ease-out`
   - Используется CSS `will-change` для оптимизации

---

## 🔄 Совместимость

Система имеет **fallback** на старую реализацию:

```javascript
if (typeof FocusCards !== 'undefined') {
    // Используем FocusCards
    focusCardsInstance = new FocusCards(...);
} else {
    // Fallback - старая система
    regionsToShow.forEach((region, index) => {
        const regionCard = createRegionCard(region, index);
        regionsGrid.appendChild(regionCard);
    });
}
```

Если компонент не загружен - проект работает как раньше!

---

## 📱 Адаптивность

### Mobile (< 768px):
- 1 колонка
- Высота карточек: `15rem` (240px)

### Tablet (768-1024px):
- 2 колонки
- Высота карточек: `24rem` (384px)

### Desktop (> 1024px):
- 3 колонки
- Высота карточек: `24rem` (384px)

---

## 🚀 API методы

### updateCards(newCards)
Заменяет все карточки новыми:
```javascript
focusCardsInstance.updateCards(newCardsData);
```

### addCards(cards)
Добавляет карточки к существующим:
```javascript
focusCardsInstance.addCards(additionalCards);
```

### clearCards()
Очищает все карточки:
```javascript
focusCardsInstance.clearCards();
```

---

## 🎯 Интеграция с поиском

FocusCards полностью совместим с системой поиска:

1. Пользователь вводит запрос
2. `searchRegions()` фильтрует регионы
3. `loadRegions()` пересоздает FocusCards с отфильтрованными данными
4. Если ничего не найдено - показывается сообщение

---

## 🔥 Производительность

### Оптимизации:
- ✅ Lazy loading для изображений
- ✅ CSS `will-change` для анимаций
- ✅ Предзагрузка следующих изображений
- ✅ Использование CSS transforms (GPU acceleration)
- ✅ Debounce для обработчиков мыши

### Память:
- Карточки добавляются постепенно (по 6 штук)
- Cleanup event listeners при `destroy()`
- Нет утечек памяти

---

## 📊 Сравнение: До и После

### До (старая система):
```javascript
regionsToShow.forEach((region, index) => {
    const regionCard = createRegionCard(region, index);
    regionsGrid.appendChild(regionCard);
});
```
- Обычные карточки
- Простой hover эффект
- Нет focus эффекта

### После (FocusCards):
```javascript
new FocusCards('regionsGrid', {
    cards: cardsData,
    onCardClick: (card) => showRegionDetails(card.id)
});
```
- Профессиональный вид из Aceternity UI
- Focus эффект при наведении
- Размытие неактивных карточек
- Плавные анимации

---

## 🎨 Кастомизация

### Изменить цвета overlay:
Отредактируй `components/aceternity/focus-cards.js`:

```javascript
const overlay = this.createElement('div', {
    className: cn(
        'focus-card-overlay',
        'absolute inset-0',
        'bg-black/50',  // Измени здесь!
        // ...
    )
});
```

### Изменить blur:
```javascript
new FocusCards('container', {
    blurAmount: '12px'  // Больше размытие
});
```

### Изменить scale:
```javascript
new FocusCards('container', {
    scaleInactive: 0.95  // Больше уменьшение
});
```

---

## 🐛 Troubleshooting

### Карточки не отображаются?
```javascript
// Проверь в консоли:
console.log(typeof FocusCards);  // Должно быть 'function'
console.log(typeof AceternityComponent);  // Должно быть 'function'
```

### Fallback на старую систему?
Убедись что скрипты загружены в правильном порядке:
1. `animations.js`
2. `aceternity-adapter.js`
3. `focus-cards.js`
4. `script.js`

### Эффект не работает?
Проверь что hover эффекты работают:
```javascript
// В консоли:
focusCardsInstance.handleMouseEnter(0);  // Наведение на первую
focusCardsInstance.handleMouseLeave();   // Убрать наведение
```

---

## 📚 Дополнительные ресурсы

- **Оригинал компонента**: https://ui.aceternity.com/components/focus-cards
- **Гайд по Aceternity UI**: `ACETERNITY_INTEGRATION_GUIDE.md`
- **Быстрый старт**: `ACETERNITY_QUICKSTART.md`
- **Демо страница**: `aceternity_showcase.html`

---

## ✅ Чек-лист интеграции

- [x] Создан компонент `focus-cards.js`
- [x] Адаптирован React код в Vanilla JS
- [x] Интегрирован в `loadRegions()`
- [x] Добавлена поддержка динамического добавления
- [x] Подключены утилиты в `index.html`
- [x] Сохранена совместимость (fallback)
- [x] Работает поиск и фильтрация
- [x] Работает "Показать больше"
- [x] Адаптивный дизайн
- [x] Оптимизирована производительность

---

Made with ❤️ for Матрешка Travel App
Inspired by [Aceternity UI](https://ui.aceternity.com/)
