# 🚀 Быстрый старт после упрощения БД

## ✅ Что было сделано

1. **Создана новая простая структура БД**
   - Таблица `travel_feed` - только для ленты путешествий
   - Убраны лишние таблицы `posts`, `qr_codes`, `trips`
   - Упрощена работа с данными

2. **Обновлен код**
   - `database.py` - новые методы для работы с `travel_feed` и Supabase Storage
   - `bot.py` - загрузка фото в Storage и сохранение в БД
   - `feed_api.py` - упрощенное API для ленты

3. **Созданы инструкции**
   - `SUPABASE_SETUP.md` - подробная настройка Supabase
   - `database_setup.sql` - SQL скрипт для создания таблицы

---

## 📋 Что нужно сделать сейчас (ШАГ ЗА ШАГОМ)

### Шаг 1: Открыть Supabase Dashboard

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в свой проект

---

### Шаг 2: Удалить старые таблицы

1. Перейдите в **Table Editor** (левое меню)
2. Удалите **ненужные** таблицы:
   - `posts` (если есть) - кликните троеточие → Delete
   - `qr_codes` (если есть) - кликните троеточие → Delete
   - Любые другие таблицы, которые вам не нужны

⚠️ **НЕ УДАЛЯЙТЕ**: `users` - эта таблица нужна!

---

### Шаг 3: Создать таблицу travel_feed

1. Перейдите в **SQL Editor** (иконка </> в левом меню)
2. Нажмите **New query**
3. Скопируйте содержимое файла `database_setup.sql` (он в корне проекта)
4. Вставьте в SQL Editor
5. Нажмите **Run** (или `Ctrl+Enter`)

✅ Должно появиться сообщение "Success. No rows returned"

---

### Шаг 4: Настроить Supabase Storage

#### 4.1. Создать bucket

1. Перейдите в **Storage** (иконка папки в левом меню)
2. Нажмите **New bucket**
3. Введите имя: `travel-photos`
4. ✅ Включите **Public bucket**
5. Нажмите **Create bucket**

#### 4.2. Настроить политики Storage

1. Нажмите на bucket `travel-photos`
2. Перейдите во вкладку **Policies**
3. Нажмите **New policy**

**Политика 1: Загрузка (INSERT)**
- Policy name: `Все могут загружать`
- Allowed operation: `INSERT`
- Target roles: `public`
- Policy definition: `true`
- Нажмите **Review** → **Save policy**

**Политика 2: Чтение (SELECT)**
- Policy name: `Все могут читать`
- Allowed operation: `SELECT`
- Target roles: `public`
- Policy definition: `true`
- Нажмите **Review** → **Save policy**

**Политика 3: Удаление (DELETE)**
- Policy name: `Все могут удалять`
- Allowed operation: `DELETE`
- Target roles: `public`
- Policy definition: `true`
- Нажмите **Review** → **Save policy**

---

### Шаг 5: Проверить .env файл

Откройте файл `.env` в корне проекта и убедитесь, что указаны правильные данные:

```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш-anon-public-ключ
BOT_TOKEN=ваш-telegram-bot-token
```

Где взять ключи:
1. **Settings** → **API** (в левом меню Supabase)
2. Скопируйте **Project URL** → это `SUPABASE_URL`
3. Скопируйте **anon public** ключ → это `SUPABASE_KEY`

---

## 🎉 Готово! Теперь можно тестировать

### Запуск бота локально:

```bash
cd /Users/alexbrizkiy/PycharmProjects/PythonProject20
python3 backend/bot.py
```

### Запуск API локально:

```bash
python3 backend/api/feed_api.py
```

---

## 🧪 Как протестировать

### 1. Тест бота:
1. Откройте Telegram
2. Найдите своего бота
3. Отправьте команду `/start`
4. Отправьте любую фотографию
5. Бот должен ответить: "✅ Отлично! Фото добавлено в ленту путешествий!"

### 2. Проверка в Supabase:
1. Откройте **Table Editor** → `travel_feed`
2. Должна появиться новая запись с:
   - telegram_id
   - username
   - photo_url (ссылка на фото в Storage)
   - created_at (время добавления)

### 3. Проверка Storage:
1. Откройте **Storage** → `travel-photos`
2. Должен появиться файл с вашим фото

### 4. Тест API:
Откройте в браузере: `http://localhost:5000/api/feed`

Должен вернуться JSON с фотографиями:
```json
{
  "success": true,
  "photos": [...],
  "count": 1
}
```

---

## 📊 Новая структура данных

### Таблица travel_feed

| Поле | Тип | Описание |
|------|-----|----------|
| **id** | число | Автоматический ID (primary key) |
| **telegram_id** | число | ID пользователя из Telegram |
| **username** | текст | Имя пользователя (@username или имя) |
| **photo_url** | текст | Ссылка на фото в Supabase Storage |
| **description** | текст | Описание фото (опционально) |
| **created_at** | время | Когда добавлено (автоматически) |

### Storage: travel-photos
- Хранит сами файлы изображений
- Публичный доступ (можно получить по URL)
- Автоматическое именование файлов: `20250114_143022_filename.jpg`

---

## 🔧 API Endpoints

### GET /api/feed
Получить ленту путешествий
- Параметры: `?limit=50&offset=0`
- Ответ: массив фотографий

### GET /api/feed/:id
Получить конкретную фотографию

### DELETE /api/feed/:id
Удалить фотографию

### GET /api/users/:telegram_id/photos
Получить фотографии конкретного пользователя

### GET /api/stats
Получить статистику (количество фото и пользователей)

### GET /api/health
Проверка работоспособности API

---

## ❓ Проблемы?

### Ошибка: "База данных не инициализирована"
- Проверьте `.env` файл
- Убедитесь, что `SUPABASE_URL` и `SUPABASE_KEY` правильные

### Ошибка: "Failed to upload photo to storage"
- Проверьте, что bucket `travel-photos` создан
- Проверьте, что bucket публичный (Public bucket = ON)
- Проверьте политики Storage

### Фото не появляется в ленте
- Проверьте таблицу `travel_feed` в Table Editor
- Проверьте логи бота (должно быть "✅ Фото добавлено в ленту")

---

## 📚 Дополнительная информация

Подробные инструкции смотрите в файле `SUPABASE_SETUP.md`

---

**Удачи! 🚀**
