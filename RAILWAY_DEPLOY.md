# 🚂 ДЕПЛОЙ НА RAILWAY - ПОШАГОВАЯ ИНСТРУКЦИЯ

## Шаг 1: Зарегистрируйся на Railway

1. Открой https://railway.app/
2. Жми "Start a New Project"
3. Логинься через GitHub

## Шаг 2: Создай новый проект

1. Нажми "New Project"
2. Выбери "Deploy from GitHub repo"
3. Выбери репозиторий `sashasasha2105/-matreshka-travel`
4. Railway автоматически определит что это Python проект

## Шаг 3: Railway автоматически задеплоит

Railway автоматически:
- Установит Python 3.11
- Установит зависимости из `requirements_photo_server.txt`
- Запустит через `Procfile` (gunicorn)
- Выделит домен типа `xxx.railway.app`

## Шаг 4: Узнай URL своего сервера

1. В Railway зайди в свой проект
2. Вкладка "Settings"
3. Найди "Domains" - там будет твой URL
4. Скопируй его (например: `matreshka-photos.railway.app`)

## Шаг 5: Обнови URL в коде

Открой `photo-storage-server.js` и замени:

```javascript
// БЫЛО:
this.serverUrl = 'http://localhost:5000';

// СТАЛО:
this.serverUrl = 'https://твой-домен.railway.app';
```

Например:
```javascript
this.serverUrl = 'https://matreshka-photos.railway.app';
```

## Шаг 6: Закоммить и запушить

```bash
git add photo-storage-server.js
git commit -m "🌐 Обновлен URL сервера на Railway"
git push origin main
```

## Шаг 7: Проверь что работает

1. Открой в браузере: `https://твой-домен.railway.app/api/health`
2. Должен вернуться JSON:
```json
{
  "status": "ok",
  "photos_count": 0,
  "photos_dir": "/app/travel_photos",
  "timestamp": "2025-01-05T12:00:00"
}
```

## Шаг 8: Тестируй приложение!

1. Открой `index.html`
2. Добавь путешествие с фотками
3. Фотки загрузятся на Railway
4. Перезагрузи страницу - фотки остались!

## 🎉 ГОТОВО!

Теперь твои фотки хранятся на Railway и доступны всегда!

---

## 📊 Лимиты Railway (бесплатный план)

- ✅ 1GB RAM - ДОСТАТОЧНО
- ✅ 1GB storage - для 20 фоток ХВАТИТ
- ✅ 500 часов в месяц - ХВАТИТ
- ✅ Автоматический деплой при пуше в GitHub

## 🔧 Если что-то не работает

### Проблема: 500 Internal Server Error

**Решение:**
1. Открой логи в Railway (вкладка "Deployments" → "View Logs")
2. Найди ошибку
3. Скорее всего проблема с зависимостями

### Проблема: Фотки не сохраняются

**Решение:**
1. Проверь что URL правильный в `photo-storage-server.js`
2. Проверь консоль браузера на ошибки CORS
3. Убедись что сервер запущен (открой `/api/health`)

### Проблема: После перезагрузки фотки пропадают

**Решение:**
Railway использует ephemeral storage (временное хранилище).
Для постоянного хранения нужно:

**Вариант 1: Cloudinary (рекомендую)**
```bash
pip install cloudinary
```

**Вариант 2: AWS S3**
```bash
pip install boto3
```

Но для твоих 20 фоток ephemeral хватит, пока не перезагружается!

## 🚀 Продвинутые фичи

### Подключить свой домен

1. В Railway: Settings → Custom Domain
2. Добавь свой домен (например: `photos.matreshka.ru`)
3. Настрой DNS записи как Railway скажет
4. Обнови URL в коде

### Посмотреть сколько фоток загружено

Открой: `https://твой-домен.railway.app/api/travel-photos`

### Удалить все фотки

```bash
curl -X DELETE https://твой-домен.railway.app/api/delete-photo/ID_ФОТКИ
```

---

**Если Railway не работает, используй альтернативы:**
- Render.com
- Heroku
- Vercel (только для Next.js, не подойдет)
- Fly.io

Удачи! 🚀
