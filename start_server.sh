#!/bin/bash

# 🚀 Автозапуск сервера Матрешка
# Этот скрипт запускает HTTP сервер для проекта

# Директория проекта
PROJECT_DIR="/Users/alexbrizkiy/PycharmProjects/PythonProject20"

# Файл для логов
LOG_FILE="$PROJECT_DIR/server.log"

# Порт сервера
PORT=8000

# Функция для логирования
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" >> "$LOG_FILE"
}

# Убиваем старый процесс если есть
if lsof -ti:$PORT > /dev/null 2>&1; then
    log "⚠️ Порт $PORT занят, останавливаем старый процесс..."
    kill -9 $(lsof -ti:$PORT) 2>/dev/null
    sleep 2
fi

# Переходим в директорию проекта
cd "$PROJECT_DIR" || exit 1

# Запускаем сервер (БЕЗ фонового режима - LaunchAgent сам управляет процессом)
log "🚀 Запуск HTTP сервера на порту $PORT..."
log "🌐 Доступен по адресу: http://localhost:$PORT"

# Запускаем сервер в foreground режиме
exec python3 -m http.server $PORT
